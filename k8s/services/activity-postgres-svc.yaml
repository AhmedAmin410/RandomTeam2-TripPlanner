apiVersion: v1
kind: Service
metadata:
  name: activity-postgres
  namespace: tripplanning
spec:
  clusterIP: None
  selector:
    app: activity-postgres
  ports:
    - port: 5432
      targetPort: 5432
