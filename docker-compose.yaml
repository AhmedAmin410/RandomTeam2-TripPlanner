services:
  postgres:
    image: postgres:16
    container_name: tripplanning-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tripplanningdb
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: