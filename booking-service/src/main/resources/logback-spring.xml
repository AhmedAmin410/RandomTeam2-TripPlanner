<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>http://loki.monitoring.svc.cluster.local:3100/loki/api/v1/push</url>
        </http>
        <format>
            <label>
                <pattern>app=tripplanning,service=booking-service,level=%level,env=k8s</pattern>
            </label>
            <message>
                <pattern>{ "timestamp": "%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}", "level": "%level", "service": "booking-service", "thread": "%thread", "logger": "%logger", "correlationId": "%X{correlationId}", "bookingId": "%X{bookingId}", "settlementId": "%X{settlementId}", "itineraryId": "%X{itineraryId}", "userId": "%X{userId}", "routingKey": "%X{routingKey}", "message": "%message" }</pattern>
            </message>
            <sortByTime>true</sortByTime>
        </format>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOKI"/>
    </root>

</configuration>