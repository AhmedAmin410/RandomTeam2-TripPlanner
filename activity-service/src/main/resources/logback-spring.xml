<configuration>
    <springProperty scope="context" name="lokiUrl" source="loki.push.url" defaultValue="http://loki.monitoring.svc.cluster.local:3100/loki/api/v1/push"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${lokiUrl}</url>
        </http>
        <format>
            <label>
                <pattern>app=tripplanning,service=activity-service,level=%level,env=k8s</pattern>
            </label>
            <message>
                <pattern>{"timestamp":"%d{ISO8601}","level":"%level","service":"activity-service","thread":"%thread","logger":"%logger{36}","correlationId":"%X{correlationId:-}","activityId":"%X{activityId:-}","itineraryId":"%X{itineraryId:-}","destinationId":"%X{destinationId:-}","routingKey":"%X{routingKey:-}","message":"%msg"}</pattern>
            </message>
        </format>
    </appender>

    <springProfile name="docker">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!docker">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="LOKI"/>
        </root>
    </springProfile>
</configuration>
